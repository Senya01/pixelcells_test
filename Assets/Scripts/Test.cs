using UnityEngine;

public class Test : MonoBehaviour
{
    public GameObject hive2;
    private float _width = 142;
    private float _height = 119;

    // void Update()
    // {
    //     float hty = hive2.transform.position.y + _height / 100 / 2;
    //     float hby = hive2.transform.position.y - _height / 100 / 2;
    //     float pty = transform.position.y + _height / 100 / 2;
    //     float pby = transform.position.y - _height / 100 / 2;
    //     float htx = hive2.transform.position.x + _width / 100 / 2;
    //     float hbx = hive2.transform.position.x - _width / 100 / 2;
    //     float ptx = transform.position.x + _width / 100 / 2;
    //     float pbx = transform.position.x - _width / 100 / 2;
    //     
    //     Debug.Log((hty <= pty && hty >= pby || hby <= pty && hby >= pby) &&
    //               (htx <= ptx && htx >= pbx || hbx <= ptx && hbx >= pbx));
    // }
}